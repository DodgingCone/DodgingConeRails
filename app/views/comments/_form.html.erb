<%= form_for([@score, @score.comments.build]) do |f| %>
  <div class="form-group">
    <%= f.label :body, class: "sr-only" %>
    <%= f.text_area :body, class: "form-control", placeholder: "Write your comment.." %>
  </div>
  <%= f.submit class: "btn btn-default" %>
<% end %>
